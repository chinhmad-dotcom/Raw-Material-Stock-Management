const fs = require('fs'); let code = fs.readFileSync('server.js', 'utf8'); code = code.replace('if (!locationCode || !currentMaterialName || !hasData) continue;', 'if(currentMaterialName===\\'Fish Oil\\') console.log(\\'AT ROW:\\', row[0], row[1], row[2], \\'loc:\\', locationCode, \\'name:\\', currentMaterialName, \\'data:\\', hasData); if (!locationCode || !currentMaterialName || !hasData) continue;'); code = code.replace(/module\.exports = \{/g, ''); code += '\nmodule.exports = { parseExcelReport };'; fs.writeFileSync('server_test2.js', code); const { parseExcelReport } = require('./server_test2.js'); parseExcelReport('G:\\\\App\\\\StockRM\\\\STOCK RAWMATERIAL REPORT 09-08-2026.xlsm');
