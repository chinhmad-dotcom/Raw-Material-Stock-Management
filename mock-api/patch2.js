const fs = require('fs'); let code = fs.readFileSync('server.js', 'utf8'); const reloadLogic = "saveSettings(settingsData);\n            if (resource === 'materials' || resource === 'silos') {\n              let newData = parseExcelReport(EXCEL_FILE_PATH);\n              if (newData) {\n                parsedData = newData;\n                syncMaterialsToSettings(parsedData.materials);\n              }\n            }"; code = code.replace(/saveSettings\(settingsData\);/g, reloadLogic); fs.writeFileSync('server.js', code);
