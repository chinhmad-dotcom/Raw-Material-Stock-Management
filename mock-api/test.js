const fs = require('fs'); let code = fs.readFileSync('server.js', 'utf8'); code = code.replace(/module\.exports = \{/g, ''); code += '\nmodule.exports = { parseExcelReport };'; fs.writeFileSync('server_test.js', code); const { parseExcelReport } = require('./server_test.js'); const data = parseExcelReport('G:\\\\App\\\\StockRM\\\\STOCK RAWMATERIAL REPORT 09-08-2026.xlsm'); console.log('LF1:', data.additives.filter(a=>a.warehouseLocation==='LF1')); console.log('FishOil:', data.additives.filter(a=>a.materialName==='Fish Oil'));
