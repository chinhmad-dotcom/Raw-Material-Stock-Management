const fs = require('fs'); const XLSX = require('xlsx'); const serverCode = fs.readFileSync('server.js', 'utf8'); const fnStr = serverCode.substring(serverCode.indexOf('function parseExcelReport'), serverCode.indexOf('// --- Dashboard Stats')); eval(fnStr); const parsed = parseExcelReport('G:\\\\App\\\\StockRM\\\\STOCK RAWMATERIAL REPORT 09-08-2026.xlsm'); console.log('LF1 in additives:', parsed.additives.find(a => a.warehouseLocation === 'LF1')); console.log('LF1 in silos:', parsed.silos.find(s => s.siloCode === 'LF1'));
